% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_generate_pseudo_absence.R
\name{generate_pseudo_absence}
\alias{generate_pseudo_absence}
\title{Generate pseudo-absence points}
\usage{
generate_pseudo_absence(data, x_col, y_col, n, min_dist = NULL)
}
\arguments{
\item{data}{A data frame containing presence points with coordinates.}

\item{x_col}{Character. Name of the longitude column.}

\item{y_col}{Character. Name of the latitude column.}

\item{n}{Integer. Number of pseudo-absence points to generate.}

\item{min_dist}{Numeric. Minimum distance to presence points to avoid overlap (optional).}
}
\value{
A data frame combining original presence points (presence = 1) and pseudo-absence points (presence = 0).
}
\description{
Creates pseudo-absence points based on observed presence data for species distribution modeling.
}
\details{
The function generates \code{n} pseudo-absence points randomly within the bounding box
defined by the presence points. If \code{min_dist} is provided, pseudo-absence points
closer than this distance to any presence point are discarded.
}
\examples{
\dontrun{
# Example presence data
data <- data.frame(
  lon = c(10, 11, 12),
  lat = c(50, 51, 52)
)

# Generate 5 pseudo-absence points
result <- generate_pseudo_absence(
  data,
  x_col = "lon",
  y_col = "lat",
  n = 5,
  min_dist = 0.5
)

head(result)
}
}
